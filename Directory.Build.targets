<Project>
  <Target Name="ComputeInformationalVersion" BeforeTargets="PrepareForBuild;GenerateNuspec">
    <Exec Command="git -C &quot;$(MSBuildThisFileDirectory).&quot; rev-parse --short HEAD"
          ConsoleToMsBuild="true"
          StandardOutputImportance="low"
          IgnoreExitCode="true">
      <Output TaskParameter="ConsoleOutput" PropertyName="GitShortSha" />
    </Exec>
    <PropertyGroup>
      <GitShortSha Condition="'$(GitShortSha)' == ''">unknown</GitShortSha>
      <InformationalVersion Condition="'$(Version)' != ''">$(Version)+$(GitShortSha)</InformationalVersion>
    </PropertyGroup>
  </Target>
</Project>
