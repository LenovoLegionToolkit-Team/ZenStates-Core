namespace ZenStates.Core.DRAM
{
    /// <summary>
    /// Slim-build stub for the original MemoryConfig (DRAM SPD/EXPO reader).
    /// Inert — only stored by Cpu, never used by the OC path.
    /// </summary>
    public class MemoryConfig
    {
        public MemoryConfig(Cpu cpuInstance) { }
    }
}
